name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_call:

permissions:
  contents: read

concurrency:
  group: agentide-ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  gate:
    name: Repository gate
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
        with:
          toolchain: 1.98.0
          components: rustfmt, clippy

      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: web/package-lock.json

      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2

      - name: Install pinned specification tools
        run: |
          tool_root="$RUNNER_TEMP/specification-tools"
          cargo install --git https://github.com/beyond10x/aep \
            --tag 0.52.0 \
            --locked --root "$tool_root" --bin aep aep-cli
          cargo install --git https://github.com/beyond10x/ess \
            --tag 0.9.2 \
            --locked --root "$tool_root" --bin ess ess-cli
          echo "$tool_root/bin" >> "$GITHUB_PATH"

      - name: Run the one repository gate
        run: cargo xtask gate
